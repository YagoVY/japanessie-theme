<div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; padding: 3rem 1rem; text-align: center; margin: 2rem 0; border-radius: 12px;">
  <h1 style="font-size: 2.5rem; margin-bottom: 1rem; text-shadow: 0 2px 10px rgba(0,0,0,0.3);">🎌 JAPANESSIE THEME WORKING!</h1>
  <p style="font-size: 1.2rem; margin-bottom: 1rem;">Your custom sections are loading correctly.</p>
  <p style="font-size: 1rem; opacity: 0.9;">Theme Version: {{ section.settings.version | default: '1.0.0' }}</p>
  
  {% if section.settings.show_details %}
    <div style="background: rgba(255,255,255,0.1); padding: 1rem; border-radius: 8px; margin-top: 1rem; backdrop-filter: blur(10px);">
      <h3>Section Status:</h3>
      <p>✅ Hero Section: {{ sections.hero_compact ? 'Loaded' : 'Missing' }}</p>
      <p>✅ Categories Section: {{ sections.product_categories ? 'Loaded' : 'Missing' }}</p>
      <p>✅ Header Section: {{ sections.header_custom ? 'Loaded' : 'Missing' }}</p>
    </div>
  {% endif %}
</div>

{% schema %}
{
  "name": "Japanessie Test",
  "tag": "section",
  "class": "test-section",
  "settings": [
    {
      "type": "text",
      "id": "version",
      "label": "Version",
      "default": "1.0.0"
    },
    {
      "type": "checkbox",
      "id": "show_details",
      "label": "Show Details",
      "default": true
    }
  ],
  "presets": [
    {
      "name": "Japanessie Test",
      "settings": {
        "version": "1.0.0",
        "show_details": true
      }
    }
  ]
}
{% endschema %}
